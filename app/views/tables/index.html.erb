<h1>All Tables</h1>
<p><%= link_to "Add a table", new_table_path %>

  <ul>
    <% @tables.each do |table| %>
      <li><%= table.name %> - <%= table.seats_taken %> of <%= table.seat_capacity %> seats taken</li>
    <% end %>
  </ul>
